<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/login_fondo" >

    <TextView
        android:id="@+id/tv_cronometro"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/ck_Route"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="30dp"
        android:text="@string/inicio_cronometro"
        android:textColor="@android:color/white"
        android:textSize="60sp" />

    <CheckBox
        android:id="@+id/ck_Route"
        android:layout_width="158dp"
        android:layout_height="158dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:background="@drawable/chk_grabar"
        android:button="@null"
        android:onClick="startStopRoute" />

</RelativeLayout>